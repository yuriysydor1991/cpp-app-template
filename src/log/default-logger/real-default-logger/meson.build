# The real logger implementation which stands behind the DefaultLogger static
# proxy. It is compiled into the default logger library itself, the very same
# way the CMake build links both object libraries into a single target.
real_default_logger_sources = files('RealDefaultLogger.cpp')
